/**
 * Step Definitions Index
 * Imports all step definition files to ensure they are loaded by Cucumber.
 */

// Import all step definition files
import './loginSteps';
import './todoSteps';

// This file ensures all step definitions are loaded when Cucumber runs
// No need to export anything as the step definitions register themselves
// with Cucumber when the files are imported
